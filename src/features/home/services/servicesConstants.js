const Options = {
    'Content-Type': 'application/x-www-form-urlencoded; charset=utf-8'
};

const LIST_SERVICE_PATH = 'url';

const servicesConstants = {
    Options,
    LIST_SERVICE_PATH,
};

export default servicesConstants;